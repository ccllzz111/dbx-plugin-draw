{
  "name": "dbx-plugin-draw-ui",
  "private": true,
  "version": "0.3.22",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:watch": "vite build --watch",
    "check": "vue-tsc --noEmit",
    "test": "vitest run",
    "prepare:math": "node scripts/prepare-math.mjs",
    "predev": "npm run verify:vendor && npm run prepare:math",
    "prebuild": "npm run verify:vendor && npm run prepare:math",
    "prebuild:watch": "npm run verify:vendor && npm run prepare:math",
    "verify:vendor": "node scripts/verify-vendor.mjs",
    "release:metadata": "node scripts/release-metadata.mjs"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@mathjax/mathjax-tex-font": "4.1.3",
    "@vitejs/plugin-vue": "^6.0.0",
    "jsdom": "^26.1.0",
    "mathjax": "4.1.3",
    "typescript": "~5.9.2",
    "vite": "^7.1.5",
    "vitest": "^3.2.4",
    "vue-tsc": "^3.0.5"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/ccllzz111/dbx-plugin-draw.git"
  },
  "engines": {
    "node": ">=22"
  }
}
